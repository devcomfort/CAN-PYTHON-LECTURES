{
  "name": "CAN",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm run --parallel build",
    "build:marp": "pnpm exec marp --html --allow-local-files --bespoke.progress -I ./slides -o ./public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@marp-team/marp-cli": "^2.5.0",
    "netlify-cli": "^15.4.1"
  }
}
